<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= user.name %>

      <% render partial:"shared/follow", locals:{user: user} %>
    </li>
  <% end %>
</ul>
